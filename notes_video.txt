
Things changed when testing:
- rocket -> mid_flight -> num_frames=500 AND takeoff: 50
- gen_objects -> init_frames + 2000 + roc_gi['frames_max'] < P.FRAMES_TOT_BODIES)

========================================================
TODO: Mon -> Wedn (1s1c each)
*Move all vids + Script to rename vids -> Rate them, select representative
*Finish Script -> Result and remainder
*Ease of use script + Conclusion script (brief)
*Record aud
*Record vid
*Edit vid
*Delay (if not done by wedn eve, then +24h, otherwise +4h Clean apt)


========================================================

watch -n 0.5 nvidia-smi

====

[Proposed video title]: "Matplotlib Animations vs Pygame: Motion Graphics Performance and Ease-of-Use Comparison"

[0 Intro draft]. As a Python programmer I, like many others, use Matplotlib for basic data visualization [001]. Visualization was always an "extension" to the core of my particular Python programming, compared to "visualization first", "code second" - people, those are "animation artists", "graphics engineers" etc.,  they work with software that is way more capable than Matplotlib, like Blender, Unity or OpenGL [002]. So here's the niche for this video: You use Python and you've probably used Matplotlib for some visualizations, and now you want to try some animation, without switching from your favorite IDE and without needing to learn Blender/Unity etc. And you're wondering, should I use Matplotlib and its Animation library for this, or should I try something else, like Pygame [003]? Because you suspect Matplotlib has its limits. I considered trying Manim as well [004], but I got this prejudice about it that it's "too mathy". PyQt was another option [005]. But it became "Matplotlib Animations versus Pygame", here is an overview of the topics that will be covered:

1. What will be shown? 2D motion graphics solar system
2. Technical set up for comparison experiments
3. Experiment RESULTS
4. Ease of use:
    Matplotlib: Simple until it isn't
    Pygame: Great with caveats
1. Conclusion

1. What will be shown? A solar system [101] So these planets and moons are png images, stored in this folder here [102], and then they are moved, rotated and modified in other ways this is called "motion graphics" or "image panning", and that is an important caveat or delimitation of this video, because this is admittedly pretty far away from Matplotlib animation's core usecase, basically the core usecase looks something like this [103], take some data points and plot them through time. But I needed image panning for this project, So if you're looking at doing an animation that includes the movement of cropped images, this video is for you, but if you only want to plot dots like this [103], it's less relevant. That having been said, this animation also includes dots, they are depicting rockets that fly between the planets, and I was thinking about including experiments where I remove the png's and only show these dot rockets, starting with a couple and ending with thousands to see how that affects performance, but I didn't do that in this video: Finding the right animation tool is highly tied to what you want to show in the animation.
2. Technical set up: Here's the codebase, you can find it on github [150] Basically I have this code here that pre-generates all the planet and moon movements and stores it in these numpy arrays, so each planet has things like xy coordinates, rotation and alphas through time etc etc, that I'm not going to go into here. So just assume that you have datastructures with all the things you would want about where a planet is, pre-generated, and now you want to use that data to animate it using either matplotlib or Pygame. [151] So here is the entry point for matplotlib and here it is for Pygame and they then use code from these two folders respectively. And as either of these front-end codes are clicked, all the settings and whatever else one may be interested in is logged in this logger file [200], so here's how that looks. So I ran 48 experiments, where each represents the generation of an animation video, and each result is stored as a row here. Here's the list of features [201] in this logger, so the columns: Matplotlib vs Pygame, more specifically Matplotlib FuncAnimation vs Pygame 2.0. There is this ArtistAnimation alternative to FuncAnimation that I've never even tried, but from what I've heard it's performance is not supposed to be any better than FuncAnimation. Let's jump down to perf_time this is the target variable, and what this is, is HOW LONG IT TAKES TO GENERATE 60 SECONDS WORTH OF ANIMATION, and REGARDLESS OF FPS, so it is normalized. Basically, the lower it is, the better, and what this is all about is looking at how all these other features impact this perf_time. So, like, Resolution: The animation was rendered in either 1920x1080 or 1280x720 resolution, but note that the objects are always rendered in the same size [202]. So as you can see, in 1280 resolution the objects look larger and this should also make the rendering a bit slower for the 1280 case, but overall I see this more as a detail than something that has a crucial effect on the results, you'll get what I mean when I get back to this in the results section. Frames Per Second, 60, 100 or 144, for most experiments it was set to 100. So I used two computers, a 10 year old laptop that's worth maybe 200 us dollars today, no GPU, and a workstation worth about 700 us dollars that houses the Nvidia RTX 3050 GPU. Both these computers are with Linux, Ubuntu distribution, but that probably doesn't affect the results too much. And then there is this WRITE parameter, it says whether the animation is shown live or written to an mp4 file. So I wanted some way to estimate the compute load of the animation, so that's what these 3 features are: Number of Png's, average diameter of the png's in pixels, and then the number of these dots. These features are then later combined in this Compute Load feature so one can generate 2D plots from it. Then there is how much RAM is used, and visual quality that I judge for each saved mp4, and number of frames rendered, that's used to see if there is any memory leak or slow down for longer videos.

I'll now say a few words about the main dependencies used within Matplotlib and Pygame, because there may be more than one way to render these moving objects. In Matplotlib, the core library used is this one [210], the transforms library and its Affine2D function. Which is definitely nailing it when it comes to ease of use: Like, for an image, first it's scaled, rotated ... here, and all one has to do in an animation is to say how much it should be scaled, rotated etc each frame. It is also possible to do this using open cv, but in a previous video I concluded that it's about 200 times slower to do that than using transforms Affine2D, so I didn't even try it here. In Pygame, the main function calls are these ones: So pygame.transforms.smoothscale and rotate. I'll go through this code in the ease-of-use section later.


4. Results.
1.Let's start with how good the output mp4 looks, I'd say they are about the same. Matplotlib is better at preserving the original png's, whereas Pygame makes it a bit more granular-looking. But main thing is that both suffer from this "stutter" problem. This is a zoom in on 1920 resolution: These planets stutter. I've spent many hours trying to figure out why, and more hours doing various complex workarounds to limit this problem.
1. So Pygame is about 10x faster to render a given video, across all compute loads.
1.FPS - doesn't help against stutter, so just use 60FPS.
1. Number of dots = Only 3% impact on compute time.
1. Computer:
1. RAM
1. Number of frames vs render time: Is there a loss of compute speed for longer videos?


- Summarize the two philosophies: "One of the hidden triumphs of matplotlib: it gives you a high-level animation and rendering API where you don’t need to know graphics fundamentals. It’s intentionally 'mathematical-plotting-first, graphics-second.' (that’s perfect—until it’s not).". "Pygame expects you to build the rendering yourself. You control the framerate, what gets drawn, how things move, and how they’re layered. In exchange for that manual labor, you get performance, flexibility, and GPU acceleration."
 - Go deeper on point above: A standard mpl user is a data scientist who is highly familiar with numpy and scipy. So that person might come in to the visualization stage with a bunch of not only data but also "data sophistication", like the idea that it would be crazy to do any computations inside the animation loop. As compared to some Pygame kid that knows nothing about data, but starts showing the game-canvas and then starts moving a hero using rect.x += 10. The biggest thing that kid learns in Pygame, is not somthing like numpy, but rather basic Object Orientation, where you have these default "Hero" objects, that have these coordinates about where the object is, when it's supposed to show and when it isn't. As with any "built-in" library structures, they have strengths and weaknesses: Strength: It allows fast computation. Weakness: See to_surface function: It's crazy that you have to remove alpha channel and then add it later + no built-in write support.
 - More on GPU: "Matplotlib still renders every frame with CPU — GPU encoding doesn’t help frame gen. Adding h264_nvenc will only shave a few seconds off for encoding (writing?), not rendering". "Each artist goes through a full transform + draw pipeline. Bigger artists = more anti-aliasing, alpha blending, CPU layout check. Matplotlib does no GPU rasterization (explain 'rasterization' more)".
- Rendering Control: Pygame is less black box, more manual. An example: In mpl, you just set blit=True and live-animation becomes faster. In Pygame, you have to set each object to blit, and this, by extension, leads you to want to understand better what blitting is doing.
- Leading to this point: Pygame assumes you will use class objects for various things to move around on the canvas, and there are built-in things for that purpose, such as Rect, Surface, and Group. Versus mpl, where the standard usecase is some Python coder who quickly wants to visualize some data, often in a single script.
- The "stutter" problem (resolution and "smoothness"). Pygame is pixel-first — all transforms happen after rasterization. E.g., you have to do int() before blitting, so it's explicitly working with ints. Matplotlib uses antialiasing and transforms at a floating point vector level before rasterization. E.g., you can feed float points into Affine2D but internally when plotting these will then become ints. This has the problem that the Matplotlib user may think that float point coordinates will be kept, when they won't. When I first encountered the "stutter" problem I thought it was some data-rounding bug on my end. So Pygame is much more clear about what level of "smoothness" one can expect in the animation (i.e. int).
- Go through the differences in the respective main functions. Include that "==" for a certain frame is convenient and that it cannot fail for this type of program because frames can't be skipped (that's more a real-time problem).



